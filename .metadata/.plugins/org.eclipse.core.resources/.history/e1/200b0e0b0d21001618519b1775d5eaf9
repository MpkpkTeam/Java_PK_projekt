package booking;

import java.math.BigDecimal;

public class ToGBP implements CashConvert{
	public BigDecimal convert(BigDecimal money) {
		RateGBP gbp=new RateGBP();
		return money.divide(gbp.getRate());
	}
}
