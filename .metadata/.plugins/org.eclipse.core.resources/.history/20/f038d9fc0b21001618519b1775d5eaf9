package booking;

import java.math.BigDecimal;

public class ToUSD implements CashConvert{
	public BigDecimal convert(BigDecimal money) {
		RateUSD usd=new RateUSD();
		return money.divide(usd.getRate());
	}
}