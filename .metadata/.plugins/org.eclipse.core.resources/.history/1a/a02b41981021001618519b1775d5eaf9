package booking;

import java.math.BigDecimal;

public class ToPLN implements CashConvert{
	public BigDecimal convert(BigDecimal money, CustomerPreferences pref) {
		return money.divide(new RatePLN().getRate());
	}
}
