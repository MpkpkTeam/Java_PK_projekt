package booking;

import java.math.BigDecimal;

public class ToEUR implements CashConvert{
	public BigDecimal convert(BigDecimal money,CustomerPreferences pref) {
		return money.divide(new ChooseCurrency(pref.getCurrency()));
	}
}